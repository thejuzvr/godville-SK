# Plan: Fix Log Errors and Unknown Action Warning

## Problems

1. **Invalid metadata for log entry: {:invalid_event_type, "memory"}** - Line 27 in state_machine.ex adds memory recall with `metadata: %{type: "memory"}`, but "memory" is not in the valid event types list in `lib/godville_sk/game/log_metadata.ex:11-21`

2. **Unknown city action: {:cautious, "..."}** - The Brain.choose_action can return `{:cautious, reason}` (brain.ex:47), but handle_idle_city doesn't have a case for it, so it falls through to the catch-all which defaults to tavern

## Solution

### Fix 1: Add "memory" to valid event types

In `lib/godville_sk/game/log_metadata.ex`:
- Add "memory" to `@event_types` list at line 11-21
- Add schema entry for "memory" in `@schemas` (empty list since no required fields)

### Fix 2: Handle :cautious action in handle_idle_city

In `lib/godville_sk/hero/state_machine.ex`:
- Add a case clause for `{:cautious, reason}` in handle_idle_city function (around line 735)
- Handle it similarly to how handle_idle_wilderness does (lines 787-793) - likely move to a new location

## Files to Modify

1. `lib/godville_sk/game/log_metadata.ex` - Add "memory" to event types
2. `lib/godville_sk/hero/state_machine.ex` - Add :cautious case in handle_idle_city

## Expected Result

- Memory recall log entries will pass validation
- Hero will handle cautious action in cities properly instead of defaulting to tavern